// This file is auto-generated by @hey-api/openapi-ts

import {
  type Client,
  formDataBodySerializer,
  type Options as Options2,
  type TDataShape,
  urlSearchParamsBodySerializer,
} from "./client";
import { client } from "./client.gen";
import type {
  CancelAgentRunData,
  CancelAgentRunErrors,
  CancelAgentRunResponses,
  CancelTeamRunData,
  CancelTeamRunErrors,
  CancelTeamRunResponses,
  ContinueAgentRunData,
  ContinueAgentRunErrors,
  ContinueAgentRunResponses,
  CreateAgentRunData,
  CreateAgentRunErrors,
  CreateAgentRunResponses,
  CreateSessionData,
  CreateSessionErrors,
  CreateSessionResponses,
  CreateTeamRunData,
  CreateTeamRunErrors,
  CreateTeamRunResponses,
  DeleteSessionData,
  DeleteSessionErrors,
  DeleteSessionResponses,
  DeleteSessionsData,
  DeleteSessionsErrors,
  DeleteSessionsResponses,
  GetAgentData,
  GetAgentErrors,
  GetAgentResponses,
  GetAgentsData,
  GetAgentsErrors,
  GetAgentsResponses,
  GetConfigData,
  GetConfigErrors,
  GetConfigResponses,
  GetSessionByIdData,
  GetSessionByIdErrors,
  GetSessionByIdResponses,
  GetSessionsData,
  GetSessionsErrors,
  GetSessionsResponses,
  GetTeamData,
  GetTeamErrors,
  GetTeamResponses,
  GetTeamsData,
  GetTeamsErrors,
  GetTeamsResponses,
  RenameSessionData,
  RenameSessionErrors,
  RenameSessionResponses,
  UpdateSessionData,
  UpdateSessionErrors,
  UpdateSessionResponses,
} from "./types.gen";

export type Options<
  TData extends TDataShape = TDataShape,
  ThrowOnError extends boolean = boolean,
> = Options2<TData, ThrowOnError> & {
  /**
   * You can provide a client instance returned by `createClient()` instead of
   * individual options. This might be also useful if you want to implement a
   * custom client.
   */
  client?: Client;
  /**
   * You can pass arbitrary values through the `meta` object. This can be
   * used to access values that aren't defined as part of the SDK function.
   */
  meta?: Record<string, unknown>;
};

class HeyApiClient {
  protected client: Client;

  constructor(args?: { client?: Client }) {
    this.client = args?.client ?? client;
  }
}

class HeyApiRegistry<T> {
  private readonly defaultKey = "default";

  private readonly instances: Map<string, T> = new Map();

  get(key?: string): T {
    const instance = this.instances.get(key ?? this.defaultKey);
    if (!instance) {
      throw new Error(`No SDK client found. Create one with "new Sdk()" to fix this error.`);
    }
    return instance;
  }

  set(value: T, key?: string): void {
    this.instances.set(key ?? this.defaultKey, value);
  }
}

export class Sdk extends HeyApiClient {
  public static readonly __registry = new HeyApiRegistry<Sdk>();

  constructor(args?: { client?: Client; key?: string }) {
    super(args);
    Sdk.__registry.set(this, args?.key);
  }

  /**
   * Get OS Configuration
   *
   * Retrieve the complete configuration of the AgentOS instance, including:
   *
   * - Available models and databases
   * - Registered agents, teams, and workflows
   * - Chat, session, memory, knowledge, and evaluation configurations
   * - Available interfaces and their routes
   */
  public getConfig<ThrowOnError extends boolean = false>(
    options?: Options<GetConfigData, ThrowOnError>,
  ) {
    return (options?.client ?? this.client).get<GetConfigResponses, GetConfigErrors, ThrowOnError>({
      security: [{ scheme: "bearer", type: "http" }],
      url: "/config",
      ...options,
    });
  }

  /**
   * Create Agent Run
   *
   * Execute an agent with a message and optional media files. Supports both streaming and non-streaming responses.
   *
   * **Features:**
   * - Text message input with optional session management
   * - Multi-media support: images (PNG, JPEG, WebP), audio (WAV, MP3), video (MP4, WebM, etc.)
   * - Document processing: PDF, CSV, DOCX, TXT, JSON
   * - Real-time streaming responses with Server-Sent Events (SSE)
   * - User and session context preservation
   *
   * **Streaming Response:**
   * When `stream=true`, returns SSE events with `event` and `data` fields.
   */
  public createAgentRun<ThrowOnError extends boolean = false>(
    options: Options<CreateAgentRunData, ThrowOnError>,
  ) {
    return (options.client ?? this.client).post<
      CreateAgentRunResponses,
      CreateAgentRunErrors,
      ThrowOnError
    >({
      ...formDataBodySerializer,
      security: [{ scheme: "bearer", type: "http" }],
      url: "/agents/{agent_id}/runs",
      ...options,
      headers: {
        "Content-Type": null,
        ...options.headers,
      },
    });
  }

  /**
   * Cancel Agent Run
   *
   * Cancel a currently executing agent run. This will attempt to stop the agent's execution gracefully.
   *
   * **Note:** Cancellation may not be immediate for all operations.
   */
  public cancelAgentRun<ThrowOnError extends boolean = false>(
    options: Options<CancelAgentRunData, ThrowOnError>,
  ) {
    return (options.client ?? this.client).post<
      CancelAgentRunResponses,
      CancelAgentRunErrors,
      ThrowOnError
    >({
      security: [{ scheme: "bearer", type: "http" }],
      url: "/agents/{agent_id}/runs/{run_id}/cancel",
      ...options,
    });
  }

  /**
   * Continue Agent Run
   *
   * Continue a paused or incomplete agent run with updated tool results.
   *
   * **Use Cases:**
   * - Resume execution after tool approval/rejection
   * - Provide manual tool execution results
   *
   * **Tools Parameter:**
   * JSON string containing array of tool execution objects with results.
   */
  public continueAgentRun<ThrowOnError extends boolean = false>(
    options: Options<ContinueAgentRunData, ThrowOnError>,
  ) {
    return (options.client ?? this.client).post<
      ContinueAgentRunResponses,
      ContinueAgentRunErrors,
      ThrowOnError
    >({
      ...urlSearchParamsBodySerializer,
      security: [{ scheme: "bearer", type: "http" }],
      url: "/agents/{agent_id}/runs/{run_id}/continue",
      ...options,
      headers: {
        "Content-Type": "application/x-www-form-urlencoded",
        ...options.headers,
      },
    });
  }

  /**
   * List All Agents
   *
   * Retrieve a comprehensive list of all agents configured in this OS instance.
   *
   * **Returns:**
   * - Agent metadata (ID, name, description)
   * - Model configuration and capabilities
   * - Available tools and their configurations
   * - Session, knowledge, memory, and reasoning settings
   * - Only meaningful (non-default) configurations are included
   */
  public getAgents<ThrowOnError extends boolean = false>(
    options?: Options<GetAgentsData, ThrowOnError>,
  ) {
    return (options?.client ?? this.client).get<GetAgentsResponses, GetAgentsErrors, ThrowOnError>({
      security: [{ scheme: "bearer", type: "http" }],
      url: "/agents",
      ...options,
    });
  }

  /**
   * Get Agent Details
   *
   * Retrieve detailed configuration and capabilities of a specific agent.
   *
   * **Returns comprehensive agent information including:**
   * - Model configuration and provider details
   * - Complete tool inventory and configurations
   * - Session management settings
   * - Knowledge base and memory configurations
   * - Reasoning capabilities and settings
   * - System prompts and response formatting options
   */
  public getAgent<ThrowOnError extends boolean = false>(
    options: Options<GetAgentData, ThrowOnError>,
  ) {
    return (options.client ?? this.client).get<GetAgentResponses, GetAgentErrors, ThrowOnError>({
      security: [{ scheme: "bearer", type: "http" }],
      url: "/agents/{agent_id}",
      ...options,
    });
  }

  /**
   * Create Team Run
   *
   * Execute a team collaboration with multiple agents working together on a task.
   *
   * **Features:**
   * - Text message input with optional session management
   * - Multi-media support: images (PNG, JPEG, WebP), audio (WAV, MP3), video (MP4, WebM, etc.)
   * - Document processing: PDF, CSV, DOCX, TXT, JSON
   * - Real-time streaming responses with Server-Sent Events (SSE)
   * - User and session context preservation
   *
   * **Streaming Response:**
   * When `stream=true`, returns SSE events with `event` and `data` fields.
   */
  public createTeamRun<ThrowOnError extends boolean = false>(
    options: Options<CreateTeamRunData, ThrowOnError>,
  ) {
    return (options.client ?? this.client).post<
      CreateTeamRunResponses,
      CreateTeamRunErrors,
      ThrowOnError
    >({
      ...formDataBodySerializer,
      security: [{ scheme: "bearer", type: "http" }],
      url: "/teams/{team_id}/runs",
      ...options,
      headers: {
        "Content-Type": null,
        ...options.headers,
      },
    });
  }

  /**
   * Cancel Team Run
   *
   * Cancel a currently executing team run. This will attempt to stop the team's execution gracefully.
   *
   * **Note:** Cancellation may not be immediate for all operations.
   */
  public cancelTeamRun<ThrowOnError extends boolean = false>(
    options: Options<CancelTeamRunData, ThrowOnError>,
  ) {
    return (options.client ?? this.client).post<
      CancelTeamRunResponses,
      CancelTeamRunErrors,
      ThrowOnError
    >({
      security: [{ scheme: "bearer", type: "http" }],
      url: "/teams/{team_id}/runs/{run_id}/cancel",
      ...options,
    });
  }

  /**
   * List All Teams
   *
   * Retrieve a comprehensive list of all teams configured in this OS instance.
   *
   * **Returns team information including:**
   * - Team metadata (ID, name, description, execution mode)
   * - Model configuration for team coordination
   * - Team member roster with roles and capabilities
   * - Knowledge sharing and memory configurations
   */
  public getTeams<ThrowOnError extends boolean = false>(
    options?: Options<GetTeamsData, ThrowOnError>,
  ) {
    return (options?.client ?? this.client).get<GetTeamsResponses, GetTeamsErrors, ThrowOnError>({
      security: [{ scheme: "bearer", type: "http" }],
      url: "/teams",
      ...options,
    });
  }

  /**
   * Get Team Details
   *
   * Retrieve detailed configuration and member information for a specific team.
   */
  public getTeam<ThrowOnError extends boolean = false>(
    options: Options<GetTeamData, ThrowOnError>,
  ) {
    return (options.client ?? this.client).get<GetTeamResponses, GetTeamErrors, ThrowOnError>({
      security: [{ scheme: "bearer", type: "http" }],
      url: "/teams/{team_id}",
      ...options,
    });
  }

  /**
   * Delete Multiple Sessions
   *
   * Delete multiple sessions by their IDs in a single operation. This action cannot be undone and will permanently remove all specified sessions and their runs.
   */
  public deleteSessions<ThrowOnError extends boolean = false>(
    options: Options<DeleteSessionsData, ThrowOnError>,
  ) {
    return (options.client ?? this.client).delete<
      DeleteSessionsResponses,
      DeleteSessionsErrors,
      ThrowOnError
    >({
      security: [{ scheme: "bearer", type: "http" }],
      url: "/sessions",
      ...options,
      headers: {
        "Content-Type": "application/json",
        ...options.headers,
      },
    });
  }

  /**
   * List Sessions
   *
   * Retrieve paginated list of sessions with filtering and sorting options. Supports filtering by session type (agent, team, workflow), component, user, and name. Sessions represent conversation histories and execution contexts.
   */
  public getSessions<ThrowOnError extends boolean = false>(
    options?: Options<GetSessionsData, ThrowOnError>,
  ) {
    return (options?.client ?? this.client).get<
      GetSessionsResponses,
      GetSessionsErrors,
      ThrowOnError
    >({
      security: [{ scheme: "bearer", type: "http" }],
      url: "/sessions",
      ...options,
    });
  }

  /**
   * Create New Session
   *
   * Create a new empty session with optional configuration. Useful for pre-creating sessions with specific session_state, metadata, or other properties before running any agent/team/workflow interactions. The session can later be used by providing its session_id in run requests.
   */
  public createSession<ThrowOnError extends boolean = false>(
    options?: Options<CreateSessionData, ThrowOnError>,
  ) {
    return (options?.client ?? this.client).post<
      CreateSessionResponses,
      CreateSessionErrors,
      ThrowOnError
    >({
      security: [{ scheme: "bearer", type: "http" }],
      url: "/sessions",
      ...options,
      headers: {
        "Content-Type": "application/json",
        ...options?.headers,
      },
    });
  }

  /**
   * Delete Session
   *
   * Permanently delete a specific session and all its associated runs. This action cannot be undone and will remove all conversation history.
   */
  public deleteSession<ThrowOnError extends boolean = false>(
    options: Options<DeleteSessionData, ThrowOnError>,
  ) {
    return (options.client ?? this.client).delete<
      DeleteSessionResponses,
      DeleteSessionErrors,
      ThrowOnError
    >({
      security: [{ scheme: "bearer", type: "http" }],
      url: "/sessions/{session_id}",
      ...options,
    });
  }

  /**
   * Get Session by ID
   *
   * Retrieve detailed information about a specific session including metadata, configuration, and run history. Response schema varies based on session type (agent, team, or workflow).
   */
  public getSessionById<ThrowOnError extends boolean = false>(
    options: Options<GetSessionByIdData, ThrowOnError>,
  ) {
    return (options.client ?? this.client).get<
      GetSessionByIdResponses,
      GetSessionByIdErrors,
      ThrowOnError
    >({
      security: [{ scheme: "bearer", type: "http" }],
      url: "/sessions/{session_id}",
      ...options,
    });
  }

  /**
   * Update Session
   *
   * Update session properties such as session_name, session_state, metadata, or summary. Use this endpoint to modify the session name, update state, add metadata, or update the session summary.
   */
  public updateSession<ThrowOnError extends boolean = false>(
    options: Options<UpdateSessionData, ThrowOnError>,
  ) {
    return (options.client ?? this.client).patch<
      UpdateSessionResponses,
      UpdateSessionErrors,
      ThrowOnError
    >({
      security: [{ scheme: "bearer", type: "http" }],
      url: "/sessions/{session_id}",
      ...options,
      headers: {
        "Content-Type": "application/json",
        ...options.headers,
      },
    });
  }

  /**
   * Rename Session
   *
   * Update the name of an existing session. Useful for organizing and categorizing sessions with meaningful names for better identification and management.
   */
  public renameSession<ThrowOnError extends boolean = false>(
    options: Options<RenameSessionData, ThrowOnError>,
  ) {
    return (options.client ?? this.client).post<
      RenameSessionResponses,
      RenameSessionErrors,
      ThrowOnError
    >({
      security: [{ scheme: "bearer", type: "http" }],
      url: "/sessions/{session_id}/rename",
      ...options,
      headers: {
        "Content-Type": "application/json",
        ...options.headers,
      },
    });
  }
}
