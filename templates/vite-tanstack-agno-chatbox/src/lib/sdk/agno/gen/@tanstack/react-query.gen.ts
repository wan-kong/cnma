// This file is auto-generated by @hey-api/openapi-ts

import {
  type InfiniteData,
  infiniteQueryOptions,
  queryOptions,
  type UseMutationOptions,
} from "@tanstack/react-query";

import { client } from "../client.gen";
import { type Options, Sdk } from "../sdk.gen";
import type {
  CancelAgentRunData,
  CancelAgentRunError,
  CancelTeamRunData,
  CancelTeamRunError,
  ContinueAgentRunData,
  ContinueAgentRunError,
  CreateAgentRunData,
  CreateAgentRunError,
  CreateSessionData,
  CreateSessionError,
  CreateSessionResponse,
  CreateTeamRunData,
  CreateTeamRunError,
  DeleteSessionData,
  DeleteSessionError,
  DeleteSessionResponse,
  DeleteSessionsData,
  DeleteSessionsError,
  DeleteSessionsResponse,
  GetAgentData,
  GetAgentError,
  GetAgentResponse,
  GetAgentsData,
  GetAgentsError,
  GetAgentsResponse,
  GetConfigData,
  GetConfigError,
  GetConfigResponse,
  GetSessionByIdData,
  GetSessionByIdError,
  GetSessionByIdResponse,
  GetSessionsData,
  GetSessionsError,
  GetSessionsResponse,
  GetTeamData,
  GetTeamError,
  GetTeamResponse,
  GetTeamsData,
  GetTeamsError,
  GetTeamsResponse,
  RenameSessionData,
  RenameSessionError,
  RenameSessionResponse,
  UpdateSessionData,
  UpdateSessionError,
  UpdateSessionResponse,
} from "../types.gen";

export type QueryKey<TOptions extends Options> = [
  Pick<TOptions, "baseUrl" | "body" | "headers" | "path" | "query"> & {
    _id: string;
    _infinite?: boolean;
    tags?: ReadonlyArray<string>;
  },
];

const createQueryKey = <TOptions extends Options>(
  id: string,
  options?: TOptions,
  infinite?: boolean,
  tags?: ReadonlyArray<string>,
): [QueryKey<TOptions>[0]] => {
  const params: QueryKey<TOptions>[0] = {
    _id: id,
    baseUrl: options?.baseUrl || (options?.client ?? client).getConfig().baseUrl,
  } as QueryKey<TOptions>[0];
  if (infinite) {
    params._infinite = infinite;
  }
  if (tags) {
    params.tags = tags;
  }
  if (options?.body) {
    params.body = options.body;
  }
  if (options?.headers) {
    params.headers = options.headers;
  }
  if (options?.path) {
    params.path = options.path;
  }
  if (options?.query) {
    params.query = options.query;
  }
  return [params];
};

export const getConfigQueryKey = (options?: Options<GetConfigData>) =>
  createQueryKey("getConfig", options);

/**
 * Get OS Configuration
 *
 * Retrieve the complete configuration of the AgentOS instance, including:
 *
 * - Available models and databases
 * - Registered agents, teams, and workflows
 * - Chat, session, memory, knowledge, and evaluation configurations
 * - Available interfaces and their routes
 */
export const getConfigOptions = (options?: Options<GetConfigData>) =>
  queryOptions<
    GetConfigResponse,
    GetConfigError,
    GetConfigResponse,
    ReturnType<typeof getConfigQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await Sdk.__registry.get().getConfig({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getConfigQueryKey(options),
  });

/**
 * Create Agent Run
 *
 * Execute an agent with a message and optional media files. Supports both streaming and non-streaming responses.
 *
 * **Features:**
 * - Text message input with optional session management
 * - Multi-media support: images (PNG, JPEG, WebP), audio (WAV, MP3), video (MP4, WebM, etc.)
 * - Document processing: PDF, CSV, DOCX, TXT, JSON
 * - Real-time streaming responses with Server-Sent Events (SSE)
 * - User and session context preservation
 *
 * **Streaming Response:**
 * When `stream=true`, returns SSE events with `event` and `data` fields.
 */
export const createAgentRunMutation = (
  options?: Partial<Options<CreateAgentRunData>>,
): UseMutationOptions<unknown, CreateAgentRunError, Options<CreateAgentRunData>> => {
  const mutationOptions: UseMutationOptions<
    unknown,
    CreateAgentRunError,
    Options<CreateAgentRunData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await Sdk.__registry.get().createAgentRun({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Cancel Agent Run
 *
 * Cancel a currently executing agent run. This will attempt to stop the agent's execution gracefully.
 *
 * **Note:** Cancellation may not be immediate for all operations.
 */
export const cancelAgentRunMutation = (
  options?: Partial<Options<CancelAgentRunData>>,
): UseMutationOptions<unknown, CancelAgentRunError, Options<CancelAgentRunData>> => {
  const mutationOptions: UseMutationOptions<
    unknown,
    CancelAgentRunError,
    Options<CancelAgentRunData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await Sdk.__registry.get().cancelAgentRun({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Continue Agent Run
 *
 * Continue a paused or incomplete agent run with updated tool results.
 *
 * **Use Cases:**
 * - Resume execution after tool approval/rejection
 * - Provide manual tool execution results
 *
 * **Tools Parameter:**
 * JSON string containing array of tool execution objects with results.
 */
export const continueAgentRunMutation = (
  options?: Partial<Options<ContinueAgentRunData>>,
): UseMutationOptions<unknown, ContinueAgentRunError, Options<ContinueAgentRunData>> => {
  const mutationOptions: UseMutationOptions<
    unknown,
    ContinueAgentRunError,
    Options<ContinueAgentRunData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await Sdk.__registry.get().continueAgentRun({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export const getAgentsQueryKey = (options?: Options<GetAgentsData>) =>
  createQueryKey("getAgents", options);

/**
 * List All Agents
 *
 * Retrieve a comprehensive list of all agents configured in this OS instance.
 *
 * **Returns:**
 * - Agent metadata (ID, name, description)
 * - Model configuration and capabilities
 * - Available tools and their configurations
 * - Session, knowledge, memory, and reasoning settings
 * - Only meaningful (non-default) configurations are included
 */
export const getAgentsOptions = (options?: Options<GetAgentsData>) =>
  queryOptions<
    GetAgentsResponse,
    GetAgentsError,
    GetAgentsResponse,
    ReturnType<typeof getAgentsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await Sdk.__registry.get().getAgents({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getAgentsQueryKey(options),
  });

export const getAgentQueryKey = (options: Options<GetAgentData>) =>
  createQueryKey("getAgent", options);

/**
 * Get Agent Details
 *
 * Retrieve detailed configuration and capabilities of a specific agent.
 *
 * **Returns comprehensive agent information including:**
 * - Model configuration and provider details
 * - Complete tool inventory and configurations
 * - Session management settings
 * - Knowledge base and memory configurations
 * - Reasoning capabilities and settings
 * - System prompts and response formatting options
 */
export const getAgentOptions = (options: Options<GetAgentData>) =>
  queryOptions<
    GetAgentResponse,
    GetAgentError,
    GetAgentResponse,
    ReturnType<typeof getAgentQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await Sdk.__registry.get().getAgent({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getAgentQueryKey(options),
  });

/**
 * Create Team Run
 *
 * Execute a team collaboration with multiple agents working together on a task.
 *
 * **Features:**
 * - Text message input with optional session management
 * - Multi-media support: images (PNG, JPEG, WebP), audio (WAV, MP3), video (MP4, WebM, etc.)
 * - Document processing: PDF, CSV, DOCX, TXT, JSON
 * - Real-time streaming responses with Server-Sent Events (SSE)
 * - User and session context preservation
 *
 * **Streaming Response:**
 * When `stream=true`, returns SSE events with `event` and `data` fields.
 */
export const createTeamRunMutation = (
  options?: Partial<Options<CreateTeamRunData>>,
): UseMutationOptions<unknown, CreateTeamRunError, Options<CreateTeamRunData>> => {
  const mutationOptions: UseMutationOptions<
    unknown,
    CreateTeamRunError,
    Options<CreateTeamRunData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await Sdk.__registry.get().createTeamRun({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Cancel Team Run
 *
 * Cancel a currently executing team run. This will attempt to stop the team's execution gracefully.
 *
 * **Note:** Cancellation may not be immediate for all operations.
 */
export const cancelTeamRunMutation = (
  options?: Partial<Options<CancelTeamRunData>>,
): UseMutationOptions<unknown, CancelTeamRunError, Options<CancelTeamRunData>> => {
  const mutationOptions: UseMutationOptions<
    unknown,
    CancelTeamRunError,
    Options<CancelTeamRunData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await Sdk.__registry.get().cancelTeamRun({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export const getTeamsQueryKey = (options?: Options<GetTeamsData>) =>
  createQueryKey("getTeams", options);

/**
 * List All Teams
 *
 * Retrieve a comprehensive list of all teams configured in this OS instance.
 *
 * **Returns team information including:**
 * - Team metadata (ID, name, description, execution mode)
 * - Model configuration for team coordination
 * - Team member roster with roles and capabilities
 * - Knowledge sharing and memory configurations
 */
export const getTeamsOptions = (options?: Options<GetTeamsData>) =>
  queryOptions<
    GetTeamsResponse,
    GetTeamsError,
    GetTeamsResponse,
    ReturnType<typeof getTeamsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await Sdk.__registry.get().getTeams({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getTeamsQueryKey(options),
  });

export const getTeamQueryKey = (options: Options<GetTeamData>) =>
  createQueryKey("getTeam", options);

/**
 * Get Team Details
 *
 * Retrieve detailed configuration and member information for a specific team.
 */
export const getTeamOptions = (options: Options<GetTeamData>) =>
  queryOptions<GetTeamResponse, GetTeamError, GetTeamResponse, ReturnType<typeof getTeamQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await Sdk.__registry.get().getTeam({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getTeamQueryKey(options),
  });

/**
 * Delete Multiple Sessions
 *
 * Delete multiple sessions by their IDs in a single operation. This action cannot be undone and will permanently remove all specified sessions and their runs.
 */
export const deleteSessionsMutation = (
  options?: Partial<Options<DeleteSessionsData>>,
): UseMutationOptions<DeleteSessionsResponse, DeleteSessionsError, Options<DeleteSessionsData>> => {
  const mutationOptions: UseMutationOptions<
    DeleteSessionsResponse,
    DeleteSessionsError,
    Options<DeleteSessionsData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await Sdk.__registry.get().deleteSessions({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export const getSessionsQueryKey = (options?: Options<GetSessionsData>) =>
  createQueryKey("getSessions", options);

/**
 * List Sessions
 *
 * Retrieve paginated list of sessions with filtering and sorting options. Supports filtering by session type (agent, team, workflow), component, user, and name. Sessions represent conversation histories and execution contexts.
 */
export const getSessionsOptions = (options?: Options<GetSessionsData>) =>
  queryOptions<
    GetSessionsResponse,
    GetSessionsError,
    GetSessionsResponse,
    ReturnType<typeof getSessionsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await Sdk.__registry.get().getSessions({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getSessionsQueryKey(options),
  });

const createInfiniteParams = <
  K extends Pick<QueryKey<Options>[0], "body" | "headers" | "path" | "query">,
>(
  queryKey: QueryKey<Options>,
  page: K,
) => {
  const params = { ...queryKey[0] };
  if (page.body) {
    params.body = {
      ...(queryKey[0].body as any),
      ...(page.body as any),
    };
  }
  if (page.headers) {
    params.headers = {
      ...queryKey[0].headers,
      ...page.headers,
    };
  }
  if (page.path) {
    params.path = {
      ...(queryKey[0].path as any),
      ...(page.path as any),
    };
  }
  if (page.query) {
    params.query = {
      ...(queryKey[0].query as any),
      ...(page.query as any),
    };
  }
  return params as unknown as typeof page;
};

export const getSessionsInfiniteQueryKey = (
  options?: Options<GetSessionsData>,
): QueryKey<Options<GetSessionsData>> => createQueryKey("getSessions", options, true);

/**
 * List Sessions
 *
 * Retrieve paginated list of sessions with filtering and sorting options. Supports filtering by session type (agent, team, workflow), component, user, and name. Sessions represent conversation histories and execution contexts.
 */
export const getSessionsInfiniteOptions = (options?: Options<GetSessionsData>) =>
  infiniteQueryOptions<
    GetSessionsResponse,
    GetSessionsError,
    InfiniteData<GetSessionsResponse>,
    QueryKey<Options<GetSessionsData>>,
    | number
    | null
    | Pick<QueryKey<Options<GetSessionsData>>[0], "body" | "headers" | "path" | "query">
  >(
    // @ts-ignore
    {
      queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<
          QueryKey<Options<GetSessionsData>>[0],
          "body" | "headers" | "path" | "query"
        > =
          typeof pageParam === "object"
            ? pageParam
            : {
                query: {
                  page: pageParam,
                },
              };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await Sdk.__registry.get().getSessions({
          ...options,
          ...params,
          signal,
          throwOnError: true,
        });
        return data;
      },
      queryKey: getSessionsInfiniteQueryKey(options),
    },
  );

/**
 * Create New Session
 *
 * Create a new empty session with optional configuration. Useful for pre-creating sessions with specific session_state, metadata, or other properties before running any agent/team/workflow interactions. The session can later be used by providing its session_id in run requests.
 */
export const createSessionMutation = (
  options?: Partial<Options<CreateSessionData>>,
): UseMutationOptions<CreateSessionResponse, CreateSessionError, Options<CreateSessionData>> => {
  const mutationOptions: UseMutationOptions<
    CreateSessionResponse,
    CreateSessionError,
    Options<CreateSessionData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await Sdk.__registry.get().createSession({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Delete Session
 *
 * Permanently delete a specific session and all its associated runs. This action cannot be undone and will remove all conversation history.
 */
export const deleteSessionMutation = (
  options?: Partial<Options<DeleteSessionData>>,
): UseMutationOptions<DeleteSessionResponse, DeleteSessionError, Options<DeleteSessionData>> => {
  const mutationOptions: UseMutationOptions<
    DeleteSessionResponse,
    DeleteSessionError,
    Options<DeleteSessionData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await Sdk.__registry.get().deleteSession({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export const getSessionByIdQueryKey = (options: Options<GetSessionByIdData>) =>
  createQueryKey("getSessionById", options);

/**
 * Get Session by ID
 *
 * Retrieve detailed information about a specific session including metadata, configuration, and run history. Response schema varies based on session type (agent, team, or workflow).
 */
export const getSessionByIdOptions = (options: Options<GetSessionByIdData>) =>
  queryOptions<
    GetSessionByIdResponse,
    GetSessionByIdError,
    GetSessionByIdResponse,
    ReturnType<typeof getSessionByIdQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await Sdk.__registry.get().getSessionById({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getSessionByIdQueryKey(options),
  });

/**
 * Update Session
 *
 * Update session properties such as session_name, session_state, metadata, or summary. Use this endpoint to modify the session name, update state, add metadata, or update the session summary.
 */
export const updateSessionMutation = (
  options?: Partial<Options<UpdateSessionData>>,
): UseMutationOptions<UpdateSessionResponse, UpdateSessionError, Options<UpdateSessionData>> => {
  const mutationOptions: UseMutationOptions<
    UpdateSessionResponse,
    UpdateSessionError,
    Options<UpdateSessionData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await Sdk.__registry.get().updateSession({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Rename Session
 *
 * Update the name of an existing session. Useful for organizing and categorizing sessions with meaningful names for better identification and management.
 */
export const renameSessionMutation = (
  options?: Partial<Options<RenameSessionData>>,
): UseMutationOptions<RenameSessionResponse, RenameSessionError, Options<RenameSessionData>> => {
  const mutationOptions: UseMutationOptions<
    RenameSessionResponse,
    RenameSessionError,
    Options<RenameSessionData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await Sdk.__registry.get().renameSession({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};
